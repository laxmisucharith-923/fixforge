[project]
name = "fixforge"
version = "0.1.0"
description = "AI-powered GitHub issue fixer (MVP)"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
